{
  "name": "quizbreaker",
  "version": "1.0.0",
  "description": "QuizBreaker is a fun, browser-based multiple-choice quiz app.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
